# Project Metadata Template

A project can be moved together with this `.defectstudio` folder without depending on system locations.

- `config` stores project-local configuration overrides.
- `assets` stores project-local assets.
- `cache` stores disposable generated data.
- `results` stores simulation or calculation outputs that should move with the project.
